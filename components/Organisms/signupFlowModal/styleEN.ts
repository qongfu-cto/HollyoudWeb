import {makeStyles} from '@mui/styles';


export const useSignupFlowStylesEN = makeStyles({
    container: {
        height: "100%",
        display: "flex",
        flexDirection: "column",
        justifyContent: "space-between",

    },

});
