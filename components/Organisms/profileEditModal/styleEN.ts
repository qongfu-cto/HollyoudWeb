import {makeStyles} from '@mui/styles';


export const useProfileEditStylesEN = makeStyles(() => ({
    container: {
        height: "100%",
        display: "flex",
        flexDirection: "column",
        justifyContent: "space-between",

    },
    stepperContainer: {
        display: "flex",
        flexDirection: "row",
        height: "100%",

        alignItems: "flex-start",
        justifyContent: "center"
    }

}))
