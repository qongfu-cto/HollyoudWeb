import {makeStyles} from '@mui/styles';

export const usePropertyRegisterLayoutStylesEN = makeStyles({
    container: {
        width: 488,
        height: 680,

        // backgroundColor:"red"
    },

});
