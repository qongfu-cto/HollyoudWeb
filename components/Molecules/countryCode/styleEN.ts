import { makeStyles } from '@mui/styles';

export const useCountryCodeStylesEN = makeStyles(props => ({
  countryContainer: {
    height: '100%',
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'row'
    // padding: "10px 0px 10px 0px "
  }
}));
