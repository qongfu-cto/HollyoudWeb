import {makeStyles} from '@mui/styles';


export const useHeaderLayoutRightSideStylesEN = makeStyles((props) => ({
    btn: {
        marginRight: 20
    },

    container: {
        width: "100%",
        height: 80,
        display: "flex",
        flexDirection: "row",
        justifyContent: "flex-end",
        alignItems: "center",
        paddingRight: 20
    }
}));
