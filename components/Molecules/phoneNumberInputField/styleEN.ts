import {makeStyles} from "@mui/styles";

export const usePhoneNumberInputFieldStylesEN = makeStyles((props) => ({
    btn: {
        marginRight: 20,
    },

    container: {

        height: "100%",
        display: "flex",
        alignItems: "center",
        flexDirection: "column",

        marginBottom: 5
    },

    input: {
        //width: 140,
        height: 50,


    },

}));
