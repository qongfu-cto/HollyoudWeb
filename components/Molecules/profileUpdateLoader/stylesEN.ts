import {makeStyles} from "@mui/styles";

export const useProfileUpdateStyles = makeStyles({
    container: {
        display: "flex",
        flexDirection: "column",
        justifyContent: "space-around",
        alignItems: "center"
    }
});
