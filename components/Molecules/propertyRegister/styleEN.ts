import {makeStyles} from '@mui/styles';


export const usePropertyRegisterStylesEN = makeStyles({


    container: {
        height: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",

    }
});
