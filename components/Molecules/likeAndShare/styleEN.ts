import {makeStyles} from '@mui/styles';

export const useLikeAndShareStylesEN = makeStyles(props => ({
    container: {
        backgroundColor: 'transparent',

        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center'
    }
}));
