import { makeStyles } from '@mui/styles';

export const useAuthenticationTitleStylesEN = makeStyles(props => ({
  container: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'flex-end',
    justifyContent: 'center'
  },
  logo: {
    height: 32,
    width: 32
  },
  imageMaintain: {
    width: 60,
    height: 50,
    marginRight: 2,
    marginLeft: 2
  }
}));
