import {makeStyles} from "@mui/styles";
import {Branding} from "../../../utilities/branding";

export const useSearchInputStylesEN = makeStyles((theme) => ({
    input: {
        backgroundColor: Branding.Colors.black[6],
    },
}));
