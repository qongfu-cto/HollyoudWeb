import { makeStyles } from '@mui/styles';

export const Styles = makeStyles(() => ({
  logoUpload: {
    marginLeft: 4,
    marginTop: -80
  },
  absolutePosition: {
    position: 'absolute'
  }
}));
