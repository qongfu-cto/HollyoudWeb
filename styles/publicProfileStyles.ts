import { makeStyles } from '@mui/styles';

export const Styles = makeStyles({
  bgText: {
    margin: 0,
    fontSize: 2,
    paddingTop: 100,
    zIndex: 100,
    minHeight: '100vh',
    display: 'flex',
    alignItems: 'center'
  }
});
